import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { CheckBox } from 'react-native-elements';

const EvidenceCheckBox = (title) => {
  return (
    <>
    </>
  );
};

const styles = StyleSheet.create({});

export default EvidenceCheckBox;

